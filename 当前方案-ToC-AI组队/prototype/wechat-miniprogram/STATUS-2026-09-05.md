# COSPAN｜合拍 小程序推进记录

日期：2026-09-05 至 09-06（跨日）。范围：协作空间 Agent 入口、请求恢复与网络排查。

## 本轮实现

1. 人机协作任务增加调研入口、简报正文、关键概念、风险与下一步建议。
2. 任务负责人采纳 / 不采纳；触发者或项目发起人取消进行中的生成。重要动作二次确认，采纳不自动完成任务。
3. 读取失败不再白屏：提供重试，保留旧内容时明确提示未更新并禁用操作。
4. 明确 AI / 模板来源、模型未配置、额度不足、生成失败、超时和网络中断。
5. 普通 API 保留 12 秒超时，模型请求单独使用 35 秒。重复点击拦截，超时后保留同一请求标识，不自动重发写请求。
6. 在已有任务卡下增加浅灰白简报区域，沿用现有主导航和视觉布局。

## 已验证及证据边界

- 当前完整工作区的小程序 `node --test test/*.test.cjs`：31 项通过。
- 后端 `node --test`：129 项通过，使用隔离的本地数据库及模拟微信 / 模型响应。
- 微信官方随开发者工具安装的 WXML / WXSS 编译器：全部 9 个模板、10 份样式编译成功；17 份 JavaScript 通过语法检查。
- IP `https://101.43.172.166/health`：200；返回 `wechat_mini_login=ready`，表示已配置，不等于本轮完成真实微信登录。
- 上述测试不等于手机实测、真实模型调用成功、最新预览包生成或审核通过。

### 提交范围核验

本轮只提交 Agent / 请求恢复相关的 10 个文件改动；已有主页面视觉迁移、未跟踪的连接 / 私聊页、素材和其他任务文档继续留在原工作区，未整体暂存。单独导出的提交快照中，本轮专项及已有 API / 领域测试 19 项通过，协作空间 WXML / WXSS 编译通过；全套 25 项中仍有 3 项既有视觉测试失败，原因是这些测试依赖未提交的发现页迁移、连接页和 Memoji 样式。后续同步完整版本时须另行检查并提交这组迁移，不能将当前独立提交视为完整可发布快照。

## 网络排查结果

- 外部 DNS 查询得到 `api.cospan.cn → 101.43.172.166`。
- 本机 DNS 返回 `198.18.0.97`，同时启用本地网络代理，可能存在 fake-IP 路由；不能只凭这一项断言 DNS 配错。
- 正式域名 HTTPS 握手失败；`--resolve` 指定真实服务器 IP 后仍失败。
- 无 SNI 连接返回 Let's Encrypt IP 证书，只包含 `101.43.172.166`，有效期到 2026-09-08 18:31:16 UTC。需进一步核实域名站点 / SNI 配置以及证书续期任务。
- 这些结果尚不足以排除本机代理或云端接入限制，需要读取服务器实际配置。没有关闭 TLS 校验，没有改生产 API 配置，也没有改云端 Nginx / 证书。

## 当前需要用户配合

### 2026-09-06 云端登录后的复核（优先于上方初步推测）

- 用户完成腾讯云 MFA 后，OrcaTerm 已连入目标实例。`nginx.service` 和 `rally.service` 均为 active，`nginx -t` 成功，`127.0.0.1:8787/health` 返回正常。
- 服务器实际已有 `api.cospan.cn` 的独立 Nginx HTTPS 站点；不能再根据此前外部握手失败推断域名站点未配置。域名证书有效至 2026-11-30 14:26:00 UTC，IP 证书已续期至 2026-09-12 07:57:42 UTC。服务器内经回环地址分别验证 IP、域名 HTTPS 均成功，未跳过证书校验。
- Mac 默认访问测试 IP 的路由经过 `utun4`，TLS 握手失败；使用 `curl --interface en0 --noproxy '*' https://101.43.172.166/health` 则重复返回 HTTP 200、TLS 校验结果 0。此证据定位到本机代理/隧道路径异常，不等于已经确认手机也经过相同路径。
- 正式域名即使从 Wi-Fi 直连真实 IP 也被重置。其 HTTP 请求返回 302，指向 `https://dnspod.qcloud.com/static/webblock.html?d=api.cospan.cn`。在浏览器读取该官方页面，明确提示“您的网站未完成备案”“使用腾讯云中国境内云资源，需在腾讯云完成备案后，方可访问”。正式接入必须解决该备案关卡，不能只切换前端地址或重新签发证书。
- 本轮只读诊断，未重启服务、修改 Nginx / 证书 / 代理规则，未修改小程序 API 地址，未生成手机新预览。恢复 Mac 开发联调需要单独处理代理路径；正式域名备案与手机真机验收仍待完成。

1. 微信开发者工具安全设置的“服务端口”关闭，CLI 无法编译 / 生成预览。已请求允许开启；不需要开启“获取登录票据”或“自动信任项目”。
2. 腾讯云 OrcaTerm 的微信扫码二次验证已于本次复核通过，目标实例可只读诊断；未关闭 MFA 或修改凭据。
3. 本轮 ADB 没有检测到手机。微信扫码预览不必依赖 USB；本轮尚未生成新二维码。

## 待完成验收

- 服务器：核实正式域名的证书、SNI 站点、备案 / 接入状态，以及模型环境配置（只核对是否配置，不输出密钥）。
- 微信平台：确认合法 request 域名和隐私配置，再切换 production。IP 开发配置不作为正式发布方案。
- 工具：完整编译，检查模拟器运行与页面截图，生成新预览包。
- 真机：两账号登录 → 发布授权资料 → 发现 / 建联 → 邀请进组 → 领取任务 → 全员确认 → 真实模型生成 → 负责人采纳 / 不采纳。
- 回归：长短左右滑动、下一张灰色卡片、四页面左侧范围按钮、开屏 / Logo、外放音效、断网恢复。
- 公开发布：主体和类目、内容治理、隐私及账号注销仍是独立发布关卡，不能以本地测试通过替代。
